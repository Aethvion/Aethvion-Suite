"""
project_mapper/db
Minimal AethvionDB subset — the five files required by Project Mapper.

Only used internally by the project_mapper package.
Do not import from here directly; use the project_mapper.* public API.
"""
